# 定时在容器内部刷新澎湃OS 官方 ROM 数据（不依赖用户访问小程序）
# 每 20 分钟刷一批 30 台「最久没更新」的机型 → 约 2 小时把所有机型轮一遍
# token 用 config.js 里的 adminToken（如需修改，这里同步改）
*/20 * * * * curl -s "http://127.0.0.1:80/api/admin/refresh?token=change-this-to-a-random-token&limit=30" > /tmp/cron-refresh.log 2>&1
